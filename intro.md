# Digital Portfolio

Check out the content pages bundled with this sample book to see more.

```{tableofcontents}
```
```{tab-set}
```{tab-item} Things I learned
- Double check your environment
- If using jupyter lab UI instead of jupyter notebook, remember to activate metadata tags by clicking on them
- Remember to clean before building again
```
```{tab-set}
```{tab-item} Things I found difficult
- Fixing Kernel path that stopped for "no reason"
- Fixing all the warnings the build shows
